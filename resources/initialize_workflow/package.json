{
  "name": "initialize_workflow",
  "version": "1.0.0",
  "description": "Function that start a Step Functions state machine execution. It uses the choreography correlation id as 'name' parameter.",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vito De Giosa",
  "license": "MIT-0",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.16.0",
    "@aws-sdk/client-sfn": "^3.16.0"
  }
}
